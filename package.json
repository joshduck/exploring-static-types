{
  "name": "typedexamples",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "check-flow": "flow",
    "build-flow": "babel src/ -d build/flow/",
    "build-typescript": "tsc",
    "build": "rimraf build/ && npm run build-flow && npm run build-typescript",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "github@joshduck.com",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.57.3",
    "rimraf": "^2.6.2",
    "typescript": "^2.5.3"
  }
}
