// @flow

function ratePizza(stars: 1 | 2 | 3 | 4 | 5) { /* ... */ }

ratePizza(5);
ratePizza(1);
ratePizza(100);
ratePizza(-1);

function setSize(size: "small" | "medium" | "large") { /* ... */ }

setSize("small");
setSize("huge");
